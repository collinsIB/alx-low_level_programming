#include "main.h"

/**
 * mul - multiple of two int
 * @a: the first number to multiply
 * @b: the second number to multiply
 *
 * Return: result a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
